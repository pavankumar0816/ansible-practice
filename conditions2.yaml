- name: Gathering facts
  hosts: local
  connection: local

  tasks:
  - name: Gathering facts
    ansible.builtin.debug:
      # msg: "{{ ansible_facts }}"

  - name: getting package manager
    ansible.builtin.debug:
      msg: "{{ ansible_facts.packages }}"
      # msg: "{{ ansible_facts.os_family }}"
       