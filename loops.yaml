- name: loops
  hosts:  local
  connection: local

  tasks:
  - name: loop through list of items
    ansible.builtin.debug:
      msg: "Items:{{ item }}"
    loop:
    # - "{{ ['item1', 'item2', 'item3'] }}"
    - Item1
    - Item2
    - Item3