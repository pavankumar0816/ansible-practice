- name: checking which user is running the playbook
  hosts: local
  connection: local
  tasks:
  - name: check user
    ansible.builtin.command: whoami
    register: user_output
  
  - name: print output
    ansible.builtin.debug:
      msg: "{{ user_output }}"